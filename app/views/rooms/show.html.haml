.pure-g
  .pure-u-1-24
  .pure-u-22-24.room-title-block
    %p
      %small
        #{ @room.name.capitalize } - #{ @room.description }

.pure-g
  .pure-u-4-5
    %message-box(messages = "#{@messages.to_json}")

    - room_member = !!(current_user && current_user.room_member?(@room))

    %message-form(is-room-member = room_member :room-id = "#{@room.id}")

  .pure-u-1-5
    %member-box
